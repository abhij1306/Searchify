# Audits router stub — endpoints implemented in B5/B6
# (audits lifecycle + executions + metrics + exports).
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["audits"])
