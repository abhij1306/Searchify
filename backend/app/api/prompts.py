# Prompts router stub — endpoints implemented in B3
# (prompt-sets + prompt CRUD + /import + /generate stub).
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["prompts"])
