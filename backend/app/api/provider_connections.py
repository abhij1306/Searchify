# Provider connections router stub — endpoints implemented in B4
# (BYOK provider-connections CRUD + /test + provider-catalog).
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["providers"])
