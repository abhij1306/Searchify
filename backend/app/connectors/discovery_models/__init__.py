# Provider-agnostic discovery-model connectors (content generation).
#
# Deliberately separate from ``answer_engines`` (measurement BYOK path): the
# content model is an env-driven app capability. The only shared code is the
# neutral ``errors.py`` classification module (content-writer.md §10).
