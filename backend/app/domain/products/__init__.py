# Product Visibility (Agentic Commerce) domain package: catalog CRUD + CSV
# import + completeness, audit-identity shim, and projection-only visibility
# reads. The deterministic product analyzer lives in ``app/analysis``.
